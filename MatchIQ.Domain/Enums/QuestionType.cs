namespace MatchIQ.Domain.Enums;

public enum QuestionType
{
    MultipleChoice,  // opciones A, B, C, D — estándar o gorilla
    CodeChallenge    // el candidato escribe código (siempre es la primera pregunta)
}
